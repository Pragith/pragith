__pycache__/
*.py[cod]
*$py.class
.env
venv/
.venv/
env/
.idea/
.vscode/
*.log
static/css/style.min.css
# Keep input css but maybe ignore output if we rebuild it
# But we might want to commit the built css for deployment ease? Spec says "This is NOT a demo project."
# Let's commit the built CSS so it runs without node in prod if needed, though Docker handles it.
# Actually, ignoring node_modules is key.
node_modules/
dist/
build/
.DS_Store
